<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@android:color/holo_blue_light</item>
        <item name="colorPrimary">#34A2DA</item>
        <item name="colorPrimaryDark">#1785C6</item>
        <item name="android:windowAnimationStyle">@style/activityAnimation</item>
    </style>

    <style name="activityAnimation" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/start_in</item>
        <item name="android:activityOpenExitAnimation">@anim/start_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/end_in</item>
        <item name="android:activityCloseExitAnimation">@anim/end_out</item>
    </style>
</resources>
